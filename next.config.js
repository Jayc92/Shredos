// NOTE: next.config.js is the active Next.js config. The tracked
// next.config.mjs is intentionally retained as a verifier-bound historical
// artifact; its /sw.js headers are currently inert because this file takes
// precedence. Do not delete or merge it without separately reviewing the
// historical verifier contracts.

/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
