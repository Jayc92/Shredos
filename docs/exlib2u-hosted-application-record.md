# EXLIB-2U — hosted staged-run application record

Recorded 2026-09-08 (UTC). The promoted, reviewed EXLIB-2U S4
staged-run package WAS EXECUTED ONCE against the hosted Supabase
project ShredOS (ref ttybyljytiwntvorugcv) by the operator path
(Joseph/ChatGPT) under the operator's explicit one-use
authorization. THE PACKAGE IS NOW SPENT — DO NOT RERUN. Claude
performed no hosted contact: every hosted fact below is
operator-path evidence supplied by the executor's preflight,
execution, and post-proof, cross-checked mechanically against the
promoted repository bytes by
scripts/verify-exlib2u-application.ts. This record approves nothing
further: the staged run is NOT approved, NOT sealed, NOT revoked,
NOT deliverable, and the S5 seal, S6 delivery configuration, and
every later act all remain separately gated.

## 1. What was executed (the exact promoted bytes)

- Package: docs/exlib2u-staged-run-package.sql, 38,582 bytes,
  sha256
  ceb4964f3537f49ef987e77876c3106917abc3edf2fc9bff3991fb644c90722f,
  at the promoted main ea8f6902b7b42a4d7f5a9af8c376900da5533e5c
  (main at execution time; the round-1 corrected candidate Codex
  accepted), carrying the annotated tag
  exlib2u-s4-staged-run-prep-reviewed-not-executed (object
  41d8eb3eb3a4ea9f811fbf9d7138158157e03f90; 89-byte annotation,
  byte-verified).
- Executed exactly once; no retry occurred after success, refusal,
  timeout, or ambiguous transport outcome. The one-use authority is
  consumed.

## 2. The hosted execution facts (operator-path evidence)

- Preflight (read-only, observed 2026-09-08T05:25:52.346242Z)
  matched the package's gates exactly: project identity, the
  promoted package fingerprint, the exact pre-state vector
  3/3/5/3/6/1/2/2/0/0/3, both freeze triggers exactly bound and
  enabled, and the four-role authority baseline at the expected
  member, grantor, and option posture. A current physical backup
  was verified present (instant 2026-09-07T13:12:01Z — see the
  chronology note below).
- Execution started 2026-09-08T05:26:08.937Z; the database created
  the run at 2026-09-08T05:26:09.940165Z; the transport returned
  2026-09-08T05:26:10.075Z with the package's surfaced result row:
  result EXLIB-2U STAGED, runs 1, run_items 6,
  staged_non_deliverable true. Post-proof completed
  2026-09-08T05:27:10.884093Z.
- Post-state vector: 3/3/5/3/6/1/2/2/1/6/3 — the vector moved in
  exactly the two run positions the package's postcondition pins
  (one run, six run items, and nothing else anywhere).
- The staged run: hosted id 6669ba78-8e75-4042-ac2a-14082a9e5940
  (hosted-generated surrogate, operator-observed and preserved
  here), run_key exlib2u-plank-release1-staged-v1, dry_run = false,
  approved_for_delivery = false, sealed_at / revoked_at /
  started_at / completed_at / result_counts all NULL, and the
  reserved product + legal approval evidence preserved exactly —
  both approver instants observed hosted as 2026-09-07T15:05:00Z,
  the same instant as the packaged 2026-09-07T11:05:00-04:00
  literal (proven by parse; the AUTHORITY-decision instant, as the
  preparation record explains).
- Membership exact: the three exercise identities plus the three
  catalog aliases — six rows, ALL_THREE_IDENTITIES.
- STRUCTURALLY NON-DELIVERABLE, verified hosted: the delivery
  gate's own five-conjunct predicate matches ZERO rows for the
  reserved key. No delivery call was made.
- DIRECTLY S5-PROMOTABLE, verified hosted: the seal validation's
  own member query reads 3 exercise members / 3 alias members /
  0 unready. NO seal was performed; the run remains unapproved and
  unsealed.

## 3. Preserved surfaces and posture (operator-path evidence)

The three approved snapshots, the three immutable review events,
the published Plank content with its fresh admission fingerprint,
and the two projected relationships all remain intact. Tenant
counts remain 84 exercises and zero tenant aliases (consistent with
no delivery ever having run: hosted tenant aliases can only arise
from delivery or user authoring). The claims invariant remains 0/0.
Both freeze triggers remain exactly bound and enabled, and the four
catalog authority memberships remain byte-for-byte at the expected
member, grantor, and option posture — authority baseline
preservation is proven, exactly the strengthened round-1 gate's
claim.

## 4. Chronology (every instant parses and orders)

The tag object's tagger instant and the recorded tag push
(2026-09-08T05:03:41Z) both PRECEDE the preflight
(05:25:52.346242Z), which precedes execution start
(05:26:08.937Z), the database-created run instant
(05:26:09.940165Z), the transport return (05:26:10.075Z), and the
post-proof (05:27:10.884093Z) — so the executed bytes were the
promoted, tagged bytes, and the run row was born inside the
execution window.

BACKUP CHRONOLOGY NOTE: the verified physical backup instant
(2026-09-07T13:12:01Z) precedes not only this staging but also the
EXLIB-2Y snapshot-review application (2026-09-08T02:17:02Z) and the
renewed snapshot decisions themselves. A restore to that backup is
AVAILABLE, NOT AUTHORIZED — and would rewind the approved
snapshots and their review events as well as this staged run; any
restore consideration is its own separately gated operator
decision. Recorded so the backup's true rewind horizon is not
misread later.

## 5. Advisors (observed, not modified)

The executor observed both hosted advisor classes and changed
nothing: 20 security notices and 48 performance notices, all
pre-existing classes (mutable function search paths, SECURITY
DEFINER exposure, RLS initialization-plan performance) left
untouched for separate adjudication. None was introduced by this
execution and none gates it.

## 6. What this changes and what it does not

The hosted database now holds EXACTLY ONE staged import run in the
Design-S4 SELECTED posture with its six-member frozen-until-seal
membership — the state one later, separately gated
exlib_approve_and_seal_run call (S5) can promote, and which the
delivery runtime cannot touch (structurally non-deliverable; the
delivery flag variables remain ABSENT everywhere; the runtime
remains inert). Nothing else changed: no snapshot, event, content,
publication, projection, claims, anatomy, alias, tenant, authority,
or environment surface moved; no delivery call, no S5 approval or
seal, no environment-variable change, no seed or inventory edit, no
Git action, no Vercel action, and no EXLIB-2S action occurred
during execution. The next gated milestones, in order and each
separately instructed: Codex review of this evidence record, its
consolidated closeout, and only then any S5 seal consideration.

## 7. Verifier lifecycle for this milestone

scripts/verify-exlib2u-application.ts (new, static, in the battery)
proves: the promoted sources and the SPENT posture (package
fingerprint live AND at the promoted tip, tag object and 89-byte
annotation, this record's DO-NOT-RERUN statement); that every
reported run-row fact equals the package's own postcondition pins
and the reserved authority artifact's values (run key, approver
identity, the instant equality of the observed
2026-09-07T15:05:00Z with the packaged EDT literal — extracted
from the artifact bytes, never restated); that the reported vector
transition and transport result equal the package's pinned gates
and surfaced SELECT; the membership, non-deliverability, and
seal-readiness claims against the package's own predicate and
query shapes; the hosted run surrogate's well-formedness and
distinctness from every previously preserved hosted UUID; the
chronology (tag object instant mechanically read from the raw tag
bytes; every reported instant parsing and ordering, the backup
rewind-horizon note included); the preservation and
authority-preservation claims' consistency with the strengthened
round-1 gates; the record's truthful boundary claims; topology;
and hygiene.

## 8. Stale-claim sweep and battery reconciliation

The sweep against a simulated commit built from the intended phase
paths enumerated exactly ONE stale historical check —
verify-exlib2u U12's HEAD-relative completed-phase topology (the
committed two-commit chain check went stale at this evidence
milestone's own commit, and its porcelain branch could not describe
this milestone's authoring state), the same recurring pattern
(ninth instance) — retargeted under the exact label `RETARGET
(EXLIB-2U hosted-application evidence)` to anchor at that phase's
own promoted tip ea8f6902b7b42a4d7f5a9af8c376900da5533e5c,
count-neutral. With it in place the simulated-commit battery and
the committed battery both read 97 suites / 7,195 checks /
0 failures — the promoted baseline 96/7,184 plus exactly this
milestone's new 11-check static suite and nothing else.

## 9. Stop condition

This milestone stops LOCAL-ONLY on its branch for Codex review. Not
pushed, not promoted, not tagged; no hosted contact by Claude at
any point; the spent package untouched; no run change, approval,
seal, delivery, environment, seed, inventory, EXLIB-2S, or
quarantine change.
