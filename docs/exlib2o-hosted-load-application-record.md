# EXLIB-2O — hosted target-snapshot load application record

Recorded 2026-09-04 (UTC). APPLICATION EVIDENCE ONLY — this record
documents that the reviewed EXLIB-2O Dead bug + Ab wheel rollout
target-snapshot load package WAS EXECUTED ONCE against the hosted
ShredOS Supabase project by the authorized operator path, and
preserves the operator-confirmed proof. The hosted execution and every
hosted check were performed by ChatGPT, NOT by Claude: Claude made no
hosted contact in this phase and never executes load packages. This
record itself approves NOTHING further: the target-snapshot gate's
adjudication, Plank database content review, eligibility admission,
publication, relationship projection, sealing, revocation, delivery,
run creation, the seed module edit, and the inventory
seed_link_compatible flip all remain separately gated acts.

## 1. Execution facts

- Executed by: ChatGPT (the Joseph/ChatGPT-only execution path; never
  by Claude), against the ShredOS Supabase project ttybyljytiwntvorugcv
  ONLY.
- Recovery point confirmed BEFORE execution: a physical backup at
  2026-09-04 13:09:27 UTC existed and was operator-confirmed current
  before the package ran.
- Executed package: docs/exlib2o-target-snapshot-load-package.sql at
  main = 632ef40f448c49e07bb7569fd6cd29cc14e62c1b (tag
  exlib2o-target-snapshot-load-prep-reviewed-not-executed, tag object
  7939f4a11d337756c80b9af57b9cf8d6137c1799), 39,230 bytes, SHA-256
  4c0d74f942da4e92efab5923a435512c750c6d794077804ffeee8c0c305c966d.
  The package file remains byte-identical after execution; any byte
  change would void its reviewed/executed status.
- The exact promoted package executed ONCE and COMMITTED successfully,
  byte-for-byte, in one transaction, with no modification, no retry,
  and no partial replay.
- Execution started: 2026-09-04T23:16:17.547Z
  (= 2026-09-04 19:16:17.547 Eastern, EDT).
- Execution finished successfully: 2026-09-04T23:16:19.483Z
  (= 2026-09-04 19:16:19.483 Eastern, EDT).
- This is a DATA-LOAD package: it creates NO migration-history entry.
  The REPOSITORY migration sequence in effect on hosted remains
  exactly 001-027.
- The package is ONE-USE by design and is now SPENT on this project:
  it must never be rerun. Its exact eleven-term pre-state gate
  (3/1/2/2/3/1/2/0/0/0/0 with both targets bare) means a second
  execution fails closed before any write or authority change, exactly
  as documented and as proven by the promoted live-harness one-use and
  refusal-matrix checks.

## 2. Pre-execution gates (operator-confirmed by ChatGPT)

Every fact in this section was observed directly by ChatGPT against
the hosted database immediately before execution:

- current_user = postgres AND session_user = postgres (the package's
  dual-identity gate).
- postgres was NOT a superuser.
- Exact initial loader membership — GRANTOR INCLUDED — was the
  reviewed hosted baseline: exactly one row for role
  exlib_catalog_loader, member postgres, grantor supabase_admin,
  ADMIN TRUE, INHERIT FALSE, SET FALSE.
- The eleven-table count vector was exactly 3/1/2/2/3/1/2/0/0/0/0
  (logical/catalog/muscles/aliases/name_claims/content/
  content_expected_relationships/relationships/import_runs/run_items/
  review_events — the package's own gate order).
- Logical identities e21b2c00-0000-4000-a000-000000000002 and
  e21b2c00-0000-4000-a000-000000000003 existed with ZERO snapshots
  (both targets bare).
- The target names were unclaimed (no claim on dead bug,
  ab wheel rollout, or ab roller rollout).
- The catalog claims invariant was 0 orphaned / 0 unclaimed.

## 3. Operator-confirmed hosted proof (post-execution)

The following facts were confirmed against the hosted database by
ChatGPT's post-execution proof. Claude did not contact the hosted
database; the EXPECTED-STATE facts below are additionally
cross-checked mechanically against the executed package's own
fail-closed postconditions and the promoted admitted authoring
artifacts by scripts/verify-exlib2o-application.ts, and the
HOSTED-STATE facts (the two hosted-generated snapshot UUIDs) are
recorded on ChatGPT's operator-path authority.

Exact post-execution counts — the eleven-table vector was exactly
3/3/5/3/6/1/2/0/0/0/0, matching the package's own fail-closed
post-state pin term for term:

- exercise_catalog_logical: 3
- exercise_catalog: 3
- exercise_catalog_muscles: 5
- exercise_catalog_aliases: 3
- exercise_catalog_name_claims: 6
- exercise_catalog_content: 1
- exercise_catalog_content_expected_relationships: 2
- exercise_catalog_relationships: 0
- exercise_catalog_import_runs: 0
- exercise_catalog_run_items: 0
- exercise_catalog_review_events: 0
- exercises (tenant): 84, unchanged.

Dead bug (exact loaded state):

- Logical UUID e21b2c00-0000-4000-a000-000000000002.
- Snapshot UUID 1ce09c1f-c13d-4231-8e12-6f35cfd761b5
  (hosted-generated by the loader's gen_random_uuid() default; a
  hosted fact, not a package literal).
- canonical_name = Dead bug, category = mobility (the human category
  decision carried by the completed review form).
- Snapshot review_status = pending with every reviewer field null;
  is_active = true; catalog_version = 1.
- Anatomy: hip_flexors / secondary (exactly one row).
- Zero aliases.

Ab wheel rollout (exact loaded state):

- Logical UUID e21b2c00-0000-4000-a000-000000000003.
- Snapshot UUID c715d840-944b-4019-b984-1687accffcf4
  (hosted-generated; a hosted fact, not a package literal).
- canonical_name = Ab wheel rollout, category = other (the human
  category decision carried by the completed review form).
- Snapshot review_status = pending with every reviewer field null;
  is_active = true; catalog_version = 1.
- Anatomy: obliques / secondary and lats / tertiary (exactly two
  rows).
- Exactly one alias: Ab roller rollout.

Bindings, claims, and invariants:

- Reverse no-swap UUID/name/category bindings hold: canonical name
  Dead bug resolves to exactly ...0002 with category mobility, and
  canonical name Ab wheel rollout resolves to exactly ...0003 with
  category other — never swapped.
- Name claims (normalized) are exactly: dead bug / canonical /
  ...0002; ab wheel rollout / canonical / ...0003; ab roller rollout /
  alias / ...0003.
- The catalog claims invariant remained 0 orphaned / 0 unclaimed.
- Both targets have ZERO instructional content (this package loads
  snapshots, never content) and ZERO projected relationships.
- Zero review events, zero import runs, zero run items.
- No database review, admission, publication, projection, delivery,
  or seed activation occurred.

## 4. Authority restoration (operator-confirmed)

- Loader membership was restored byte-for-byte to the original
  supabase_admin -> postgres baseline: exactly ONE
  exlib_catalog_loader membership remains for postgres, grantor
  supabase_admin, ADMIN TRUE, INHERIT FALSE, SET FALSE.
- The temporary postgres-granted SET membership is ABSENT.
- pg_has_role('postgres','exlib_catalog_loader','SET') = false.
- No persistent authority widening occurred: the package's
  transaction-contained elevation was created and exactly restored
  inside the single committed transaction, precisely as reviewed.

## 5. Execution-output precision (what the SQL response surfaced)

- The hosted SQL response surfaced the FINAL loader result only — the
  Ab wheel rollout call: alias_rows = 1, anatomy_rows = 2, logical
  UUID e21b2c00-0000-4000-a000-000000000003, snapshot UUID
  c715d840-944b-4019-b984-1687accffcf4.
- It did NOT separately surface the first (Dead bug) loader result,
  and this record does NOT fabricate one. No Dead bug loader-return
  payload exists in the evidence.
- That both snapshots committed successfully is proven WITHOUT the
  missing payload: the committed package's own transaction-level
  postconditions (the exact post-state vector, both complete
  per-target binding gates, and the no-swap gates — any failure rolls
  back everything) held through COMMIT, and ChatGPT's independent
  post-state queries in section 3 confirm both rows hosted-side. The
  surfaced snapshot UUID matches the section-3 Ab wheel rollout row
  exactly, and the surfaced alias_rows = 1 / anatomy_rows = 2 equal
  the package's own alias and anatomy argument cardinalities.

## 6. The target-snapshot gate: evidence produced, adjudication not performed

The promoted EXLIB-2K application record's section 5 established the
fail-closed TARGET-SNAPSHOT GATE: hosted proof that
e21b2c00-0000-4000-a000-000000000002 and
e21b2c00-0000-4000-a000-000000000003 carry active canonical snapshots
named Dead bug and Ab wheel rollout — never swapped, missing,
inactive, or ambiguous — before any review, admission, or publication
of the loaded Plank content. Section 3 of THIS record is exactly that
hosted proof, on ChatGPT's operator-path authority. This record
PRODUCES the gate's evidence; it does not ADJUDICATE the gate:
whether the gate is satisfied is a Codex/operator ruling that remains
open, and Plank database content review, eligibility admission, and
publication remain BLOCKED and NOT BEGUN — no review event exists, no
admission occurred, no publication occurred.

## 7. Plank neutrality and boundary freezes

- The entire pre-existing Plank surface — snapshot, content row,
  aliases, claims, and expected relationships — is byte/value
  untouched. The Plank content row remains pending / draft /
  unadmitted with every reviewer and admission field null.
- The two expected-relationship rows still carry the INTENDED
  assignments only (substitution -> ...0002, progression -> ...0003);
  ZERO projected relationships exist.
- The tenant exercises table remained exactly 84 rows, unchanged.
- Nothing in the runtime, API, UI, dependencies, or configuration
  changed. The seed module is byte-unchanged; the inventory's Plank
  seed_link_compatible remains false; the authored batch artifacts,
  completed review forms, admitted Plank artifact, migration 027, and
  the SPENT EXLIB-2K package are all byte-frozen.

## 8. Hosted advisors (run by ChatGPT, never Claude)

- BOTH hosted advisor classes — the Supabase SECURITY advisor and the
  Supabase PERFORMANCE advisor — were run by ChatGPT immediately
  after this execution.
- NEITHER advisor result is claimed to be globally clean. This record
  states only what was established: NEITHER advisor produced an
  execution failure or a migration-blocking finding attributable to
  this two-snapshot load.
- SECURITY's RLS-enabled-with-no-policy INFO notices on the catalog
  tables reflect the INTENTIONAL deny-by-default posture: RLS enabled,
  zero policies, client DML revoked — exactly the reviewed design.
  That posture is preserved precisely, not "fixed".
- Broader pre-existing security/performance notices remain
  UNADJUDICATED and OUTSIDE EXLIB-2O: they were NOT introduced by this
  load, NOT fixed by it, and NOT accepted by this record; they belong
  to their own future operator decisions.

## 9. Verifier lifecycle for this milestone

- scripts/verify-exlib2o.ts carried the load-preparation phase's HEAD
  topology (exactly three commits over the promoted R6 source). Its
  G1/G2/G4 topology proofs are revised under the explicit label
  `RETARGET (EXLIB-2O hosted-execution evidence)`: they are anchored
  to the promoted EXLIB-2O tip
  632ef40f448c49e07bb7569fd6cd29cc14e62c1b (whose tree provably
  contains no application record), where they were and remain true.
  The package header's PREPARED — NOT EXECUTED status, the
  load-preparation record's not-executed statements, and the promoted
  tag annotation are byte-frozen history that remain true AS WRITTEN
  of their own phase; no historical proof was weakened, and the
  suite's totals are unchanged (35/0). The suite's uncommitted-state
  authoring-scope check is superseded by this milestone's own
  phase-boundary check in the application verifier.
- scripts/verify-exlib2o-application.ts (new) owns the executed-state
  posture from this milestone forward: execution facts pinned verbatim
  with ChatGPT attribution, the recovery point, the expected-state
  facts cross-checked against the executed package's own gates and
  the promoted admitted artifacts, the execution-output precision
  (final-loader-result-only, no fabricated Dead bug payload), the
  gate evidence/adjudication distinction held open, authority
  restoration pinned grantor-included, advisor precision enforced,
  boundaries re-proven, and the lifecycle two-state check (no
  application record at the promoted tip; exactly this one in the
  live tree).

## 10. Dependency map (later, explicitly gated)

1. Codex review of this evidence milestone; push, promotion, and tag
   are separate explicit gates.
2. Codex/operator adjudication of the TARGET-SNAPSHOT GATE on the
   section-3 evidence.
3. Plank database content review, eligibility admission, and
   publication of the loaded content remain their own authority-gated
   acts, each requiring explicit instruction, and remain BLOCKED until
   the gate adjudication.
4. The seed module edit and the inventory seed_link_compatible flip
   remain facts of the later coordinated delivery-activation release.
5. Any future catalog loading beyond these two snapshots requires its
   own authored, reviewed, and admitted artifacts and its own reviewed
   load package; this package is SPENT and must never be rerun.
