# EXLIB-2Y — hosted snapshot-review application record

Recorded 2026-09-08 (UTC). The promoted, reviewed EXLIB-2Y
application package WAS EXECUTED ONCE against the hosted Supabase
project ShredOS (ref ttybyljytiwntvorugcv) by ChatGPT under the
operator's explicit one-use instruction. THE PACKAGE IS NOW SPENT —
DO NOT RERUN. Claude performed no hosted contact: every hosted fact
below is operator-path evidence supplied by ChatGPT's preflight,
execution, and post-proof, cross-checked mechanically against the
promoted repository bytes by scripts/verify-exlib2y-application.ts.
This record approves nothing further: no import run exists, nothing
is sealed, no delivery occurred, and the S4/S5/S6 gates all still
stand.

## 1. What was executed (the exact promoted bytes)

- Package: docs/exlib2y-snapshot-review-application-package.sql,
  25,193 bytes, sha256
  74934419d027b477933f01bcbd2f4984700b34c6416ecd9c43c64bbd528757e0,
  at the promoted candidate 5fc27b8d63e1a6498eb7866136b9f04647b8cab8
  (main at execution time), carrying the annotated tag
  exlib2y-snapshot-review-application-prep-reviewed-not-executed
  (object 5df3edc2f7c465d8bd79f43a4079e7cc47bc174a; 84-byte
  annotation, byte-verified). Codex reviewed and accepted the
  package as-is before execution.
- Executed exactly once; no retry occurred. The one-use authority is
  consumed.

## 2. The hosted execution facts (operator-path evidence)

- Preflight matched the package's gates exactly: three active
  PENDING snapshots with NULL audit tuples, ZERO review events, the
  expected publication/projection state, and the state vector
  3/3/5/3/6/1/2/2/0/0/0.
- Execution surfaced the package's result row: EXLIB-2Y APPLIED with
  approved_snapshots = 3 and review_events = 3.
- Database effect timestamp: 2026-09-08T02:17:02.173075Z. Post-proof
  completed: 2026-09-08T02:17:50.698335Z. Both instants follow the
  closeout tag publication (tag object instant 2026-09-08T02:08:00Z;
  recorded tag push 2026-09-08T02:08:45Z) — the executed bytes were
  the promoted, tagged bytes.
- Final state vector: 3/3/5/3/6/1/2/2/0/0/3 — the vector moved in
  exactly the one position the package's postcondition pins (three
  review events and nothing else anywhere).
- All three snapshots transitioned pending -> approved carrying
  Joseph Carfagno's exact human tuple at the decision instant
  2026-09-07T23:06:00Z (the hosted UTC representation of the
  packaged 2026-09-07T19:06:00-04:00 literal — the same instant).
- Hosted row and event identities (hosted-generated surrogates,
  operator-observed):
  - Plank: snapshot ca566325-8d0d-4152-a15d-63baa065ac1d, event
    33e180fb-e94f-485a-ba2b-b7aa9a974579.
  - Dead bug: snapshot 1ce09c1f-c13d-4231-8e12-6f35cfd761b5, event
    4d01df9d-8a4e-496b-b7e9-32529e12ffd8.
  - Ab wheel rollout: snapshot c715d840-944b-4019-b984-1687accffcf4,
    event 081f696a-f79b-4bd3-8c13-6b5669dc3022.
- Preserved surfaces confirmed unchanged: governed snapshot fields,
  publication/admission state, relationships, tenant counts,
  authority memberships, and the client-denial posture.

## 3. Surrogate cross-consistency and a provenance-note disclosure

The three hosted snapshot surrogates observed at execution agree
EXACTLY with the promoted evidence trail: Dead bug and Ab wheel
rollout match the EXLIB-2O application record's preserved literals,
and Plank matches the EXLIB-2Q application record's preserved
literal (which that record first evidenced). This three-way
agreement (2O/2Q records, the package's logical-identity
resolution, and today's operator observation) is exactly what the
fixture-portability rule predicts.

DISCLOSURE: the EXLIB-2V/2W snapshot-review forms' provenance note
for Plank stated its hosted snapshot UUID was "hosted-generated
... NOT preserved in promoted evidence". That note was FACTUALLY
WRONG at authoring time: it cited only the EXLIB-2K load record
(which indeed preserved no surrogate), but the later EXLIB-2Q
admission record — promoted well before the forms were authored —
had already preserved ca566325-8d0d-4152-a15d-63baa065ac1d. The
error is NON-GOVERNING and voids nothing: the surrogate was never
one of the eighteen governed fields the human decision approved,
the forms' resolution rule (governed logical identity, never a
surrogate) was and remains correct precisely because surrogates
are not decision inputs, the package contained no surrogate
literal, and the applied transitions matched every governed field.
It is recorded here so the provenance claim is not repeated as if
true.

## 4. Advisors (observed, not modified)

ChatGPT's post-execution pass observed the hosted database
advisors without modifying anything. They report pre-existing
security/performance recommendations — mutable function search
paths, SECURITY DEFINER exposure to authenticated callers, and RLS
initialization-plan performance — none introduced by this
execution and none a gate for it. They are recorded as
observations for future, separately gated work only.

## 5. What this changes and what it does not

The three catalog snapshots are now APPROVED with complete audit
tuples and immutable trigger-created events — so the S5 seal's
member validation (every exercise member approved, active, fully
review-audited) is now SATISFIABLE for a future run whose
membership binds these identities. Nothing else changed: NO import
run exists (runs and run items remain 0), nothing is approved or
sealed at the run level, no delivery occurred, no environment
variable, seed, inventory, or authority changed, and the delivery
runtime remains inert behind the absent flag. The next gated
milestone after this evidence record is the EXLIB-2U S4 staged-run
preparation on a fresh branch from the then-promoted main, using
the reserved authority inputs (run key
exlib2u-plank-release1-staged-v1; membership ALL_THREE_IDENTITIES)
— followed by its own review, hosted execution, and evidence
before S5 sealing is ever considered.

## 6. Verifier lifecycle for this milestone

scripts/verify-exlib2y-application.ts (new, static, in the battery)
proves: the promoted sources and the SPENT posture (package
fingerprint, tag bytes, this record's DO-NOT-RERUN statement); that
every reported tuple fact equals the immutable decision artifacts
(reviewer, rationale, and the instant equality of
2026-09-07T23:06:00Z with the packaged EDT literal — extracted from
the artifact bytes, never restated); that the reported vector
transition equals the package's own pinned pre/post vectors; the
surrogate cross-consistency (the two target UUIDs equal the 2O
record's literals and the Plank UUID equals the 2Q record's
literal) plus well-formedness and distinctness of all six reported
identities; the chronology (execution instant after the tag object
instant, mechanically read); the provenance-note disclosure's
presence and its non-governing scope; the record's truthful
boundary claims (spent, no run, no seal, no delivery, no repo
mutation by the executor); topology; and hygiene.

## 7. Stale-claim sweep and battery reconciliation

The sweep against a simulated commit built from the intended phase
paths enumerated exactly ONE stale historical check —
verify-exlib2y Y11's HEAD-relative completed-phase topology, the
same recurring pattern — retargeted under the exact label
`RETARGET (EXLIB-2Y hosted-application evidence)` to anchor at that
phase's own promoted tip 5fc27b8d63e1a6498eb7866136b9f04647b8cab8,
count-neutral. With it in place the simulated-commit battery and
the committed battery both read 95 suites / 7,168 checks /
0 failures — the promoted baseline 94/7,159 plus exactly this
milestone's new 9-check static suite and nothing else.

## 8. Stop condition

This milestone stops LOCAL-ONLY on its branch for Codex review. Not
pushed, not promoted, not tagged; no hosted contact by Claude at
any point; the spent package untouched; no run, approval, seal,
delivery, environment, seed, inventory, EXLIB-2S, or quarantine
change.
