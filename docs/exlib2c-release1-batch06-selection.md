# EXLIB-2C Release-1 Batch 6 — completion record (Thruster)

Prepared 2026-08-28. Documents the sixth and final ordinary
authoring phase of the 127-entry release-1 inventory. This record
approves nothing; the record remains pending with null review
evidence.

## Derivation (no selection algorithm required)

The input corpus is exactly the promoted 125-record Batch 1-5
corpus. Removing those 125 names and the separately gated Plank
from the 127 non-deferred release-1 identities leaves exactly one
ordinary entry: Thruster. No entry was hand-picked and no scoring
choice existed — the eligible ordinary set had cardinality one, so
Batch 6 is Thruster by derivation, not selection.

Plank remains excluded because its live seed is bodyweight-tracked
while the catalog proposal is timed, and no reconciliation of that
divergence has been approved; it is gated for its own later phase
and is NOT counted as an ordinary remainder. All 8 deferred
weight_time entries remain excluded pending the paused weight_time
implementation.

## Equipment note

The promoted inventory pins Thruster to equipment=barbell, so the
authored default is the barbell thruster taken from rack hooks;
the dumbbell-based practice option appears only as an explicitly
labeled regression in the accessibility field.

## Machine-checkable summary

```json
{
 "batch": 6,
 "batch_size": 1,
 "input_corpus": 125,
 "cumulative_ordinary_authored": 126,
 "remaining_ordinary_unauthored": 0,
 "batch6_entries": [
  "Thruster"
 ],
 "selection_discretion": "none - the eligible ordinary set had cardinality one",
 "plank_gated_separately": true,
 "plank_not_an_ordinary_remainder": true,
 "deferred_weight_time_excluded": 8,
 "completes": "ordinary authoring only - not specialist review, approval, eligibility, publication, loading, implementation, or the full release-1 corpus"
}
```

## Accounting

- Ordinary release-1 entries authored after Batch 6: 126 of 126
  (all non-deferred identities except the gated Plank).
- Ordinary entries remaining: none.
- Remaining non-deferred release-1 identity: Plank only, still
  gated on its seed-reconciliation review.
- Deferred weight_time entries: 8, unchanged.

Specialist review is a later explicit gate; nothing in this record
authorizes approval, loading, publication, or import eligibility.
